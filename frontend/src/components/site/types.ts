/**
 * Re-export types for site components
 */
export type {
  PublicCompany as Company,
  PublicService as Service,
  PublicServiceCategory as ServiceCategory,
  PublicWebsiteSection as WebsiteSection,
} from '@/lib/public-api'

export type { IndustryTheme } from '@/lib/themes'
