# Система кастомизации дизайна сайта

## Концепция

**Разделение ответственности:**
- **Тема** = определяет ФОРМЫ (border-radius, тени, паттерны)
- **Цвета** = выбираются отдельно (3 цвета)
- **Шрифты** = выбираются отдельно (заголовки + текст)

---

## 1. Система трёх цветов

| Цвет | Назначение | Где используется |
|------|------------|------------------|
| **Primary** | Основной брендовый | Кнопки, ссылки, активные состояния, ключевые акценты |
| **Secondary** | Вторичный | Hover-состояния, бордеры, иконки, второстепенные акценты |
| **Background** | Фон | Фон страницы, секций, карточек (с вариациями оттенков) |

### Автогенерация оттенков

Из каждого базового цвета автоматически создаются оттенки:

```
Primary #e91e63 →
  50:  светлый фон карточек
  100: легкий hover
  500: основной цвет
  600: hover кнопок
  700: pressed состояние
  900: текст на светлом фоне

Background #fffbeb →
  основной:     фон страницы
  альтернатива: чередующиеся секции
  surface:      карточки (обычно белый)
```

### CSS переменные

```css
:root {
  /* Primary */
  --color-primary-50: #fce4ec;
  --color-primary-100: #f8bbd9;
  --color-primary-500: #e91e63;
  --color-primary-600: #d81b60;
  --color-primary-700: #c2185b;
  --color-primary-900: #880e4f;
  --color-primary-contrast: #ffffff;

  /* Secondary */
  --color-secondary-100: #e1bee7;
  --color-secondary-500: #9c27b0;
  --color-secondary-600: #8e24aa;

  /* Background */
  --color-background: #fffbeb;
  --color-background-alt: #fef3c7;
  --color-surface: #ffffff;

  /* Text (автоматически) */
  --color-text: #1f2937;
  --color-text-muted: #6b7280;
}
```

---

## 2. Темы (только формы)

Тема определяет визуальный стиль через формы, НЕ через цвета.

| Тема | border-radius | Тени | Характер форм |
|------|---------------|------|---------------|
| **Cosmetology** | 24px мягкие | Размытые, легкие | Плавные волны, градиенты |
| **Medical** | 4px острые | Четкие, минимальные | Прямые линии, чистота |
| **Massage** | 16px | Средние | Органические, природные |
| **Sport** | 0-8px | Резкие, offset | Диагонали, углы |
| **Beauty** | 9999px (pill) | Glow-эффекты | Элегантные овалы |
| **Wellness** | 12px | Мягкие | Минимализм, воздух |

### Структура темы

```typescript
interface Theme {
  borderRadius: {
    card: string      // '24px'
    button: string    // '12px'
    input: string     // '8px'
  }
  shadow: {
    card: string      // '0 10px 40px rgba(0,0,0,0.06)'
    button: string    // 'none'
  }
  heroStyle: string   // 'gradient-wave' | 'clean-minimal' | 'diagonal-split'
  buttonStyle: string // 'rounded' | 'sharp' | 'pill' | 'bold'
  spacing: string     // 'spacious' | 'compact' | 'tight'
}
```

---

## 3. Шрифты

| Тип | Применение |
|-----|------------|
| **Accent Font** | H1, H2, H3, Hero-текст |
| **Body Font** | Параграфы, списки, кнопки, инпуты |

### Рекомендуемые пары

- **Playfair Display + Inter** — элегантный + современный
- **Montserrat + Open Sans** — современный + читабельный
- **Oswald + Roboto** — динамичный + нейтральный
- **Cormorant + Lato** — классический + чистый

### CSS

```css
:root {
  --font-accent: 'Playfair Display', serif;
  --font-body: 'Inter', sans-serif;
}

h1, h2, h3 { font-family: var(--font-accent); }
body, p, button { font-family: var(--font-body); }
```

---

## 4. Применение в компонентах

### До (хардкод)
```tsx
<button className="bg-pink-500 hover:bg-pink-600 rounded-lg">
```

### После (CSS переменные)
```tsx
<button
  className="rounded-[var(--radius-button)]"
  style={{
    backgroundColor: 'var(--color-primary-500)',
  }}
>
```

### Или Tailwind arbitrary values
```tsx
<button className="bg-[var(--color-primary-500)] hover:bg-[var(--color-primary-600)]">
```

---

## 5. UI в админке

```
┌─────────────────────────────────────────────────┐
│ 🎨 Кольори                                      │
├─────────────────────────────────────────────────┤
│ Основний       [● ● ● ● ● ● ●] #e91e63          │
│ Вторинний      [● ● ● ● ● ● ●] #9c27b0          │
│ Фон            [□ □ □ □ □ □ □] #fffbeb          │
├─────────────────────────────────────────────────┤
│ 🔤 Шрифти                                       │
├─────────────────────────────────────────────────┤
│ Заголовки      [Playfair Display ▼]             │
│ Текст          [Inter ▼]                        │
├─────────────────────────────────────────────────┤
│ 📐 Стиль форм                                   │
├─────────────────────────────────────────────────┤
│ ○ Косметологія  (м'які округлі форми)           │
│ ○ Медицина      (чіткі прямі лінії)             │
│ ○ Масаж         (природні форми)                │
│ ○ Спорт         (динамічні кути)                │
│ ○ Б'юті         (елегантні пілюлі)              │
│ ○ Веллнес       (мінімалізм)                    │
└─────────────────────────────────────────────────┘
```

---

## 6. Что нужно реализовать

### Backend
- [ ] Добавить `secondary_color` в модель Company
- [ ] Миграция 019

### Site (публичный сайт)
- [ ] Создать `site/src/lib/colors.ts` — генерация оттенков из HEX
- [ ] Обновить `site/src/lib/themes.ts` — убрать цвета, оставить только формы
- [ ] В `page.tsx` — инжектировать CSS переменные в `<style>`
- [ ] Обновить компоненты — использовать CSS переменные

### Admin Panel
- [ ] Обновить UI для выбора 3 цветов (Primary, Secondary, Background)

---

## 7. Утилита генерации оттенков

```typescript
// site/src/lib/colors.ts

function hexToHsl(hex: string): { h: number; s: number; l: number } {
  // конвертация HEX → HSL
}

function hslToHex(hsl: { h: number; s: number; l: number }): string {
  // конвертация HSL → HEX
}

export function generateShades(hex: string) {
  const hsl = hexToHsl(hex)
  return {
    50: hslToHex({ ...hsl, l: 95 }),
    100: hslToHex({ ...hsl, l: 85 }),
    200: hslToHex({ ...hsl, l: 75 }),
    300: hslToHex({ ...hsl, l: 65 }),
    400: hslToHex({ ...hsl, l: 55 }),
    500: hex,
    600: hslToHex({ ...hsl, l: 45 }),
    700: hslToHex({ ...hsl, l: 35 }),
    800: hslToHex({ ...hsl, l: 25 }),
    900: hslToHex({ ...hsl, l: 15 }),
  }
}

export function generateCssVariables(config: {
  primary: string
  secondary: string
  background: string
  accentFont: string
  bodyFont: string
}): string {
  const p = generateShades(config.primary)
  const s = generateShades(config.secondary)
  const bg = generateShades(config.background)

  return `
    :root {
      --color-primary-50: ${p[50]};
      --color-primary-100: ${p[100]};
      --color-primary-500: ${p[500]};
      --color-primary-600: ${p[600]};
      --color-primary-700: ${p[700]};
      --color-primary-900: ${p[900]};

      --color-secondary-100: ${s[100]};
      --color-secondary-500: ${s[500]};
      --color-secondary-600: ${s[600]};

      --color-background: ${config.background};
      --color-background-alt: ${bg[100]};
      --color-surface: #ffffff;

      --color-text: #1f2937;
      --color-text-muted: #6b7280;

      --font-accent: '${config.accentFont}', sans-serif;
      --font-body: '${config.bodyFont}', sans-serif;
    }
  `
}
```
