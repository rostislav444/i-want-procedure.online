# Концепція стилізації публічних сайтів

## Основна ідея

Розділення **ФОРМ** (тема) від **КОЛЬОРІВ** (налаштування компанії).

```
┌─────────────────────────────────────────────────────────────┐
│                    ПУБЛІЧНИЙ САЙТ                           │
├─────────────────────────────────────────────────────────────┤
│  ТЕМА (industry_theme)     │  КОЛЬОРИ (company settings)   │
│  ─────────────────────     │  ──────────────────────────   │
│  • border-radius           │  • primary_color              │
│  • shadows                 │  • secondary_color            │
│  • hero style              │  • background_color           │
│  • card style              │  • accent_font                │
│  • button style            │  • body_font                  │
│  • patterns                │                               │
└─────────────────────────────────────────────────────────────┘
```

## Система трьох кольорів

| Колір | Призначення | Використання |
|-------|-------------|--------------|
| **Primary** | Основний брендовий | Кнопки, посилання, акценти, ціни |
| **Secondary** | Вторинний акцент | Градієнти, hover-ефекти, декор |
| **Background** | Фон сторінки | Фон секцій, карток, чередування |

### Автоматична генерація відтінків

З кожного базового кольору генеруються відтінки:

```
Primary #e91e63 →
  --color-primary-50:  #f8f0f3  (дуже світлий, фон карток)
  --color-primary-100: #f3c7d6  (світлий, hover)
  --color-primary-200: #eca0b8
  --color-primary-500: #e91e63  (основний)
  --color-primary-600: #d81b60  (темніший hover)
  --color-primary-700: #c2185b  (pressed)
  --color-primary-900: #880e4f  (текст)
  --color-primary-contrast: #ffffff (текст на primary)
```

### CSS змінні

```css
:root {
  /* Primary */
  --color-primary-50: #f8f0f3;
  --color-primary-100: #f3c7d6;
  --color-primary-500: #e91e63;
  --color-primary-600: #d81b60;
  --color-primary-700: #c2185b;
  --color-primary-contrast: #ffffff;

  /* Secondary */
  --color-secondary-100: #e1bee7;
  --color-secondary-500: #9c27b0;
  --color-secondary-600: #8e24aa;

  /* Background */
  --color-background: #ffffff;
  --color-background-alt: #f5f5f5;
  --color-surface: #ffffff;

  /* Text */
  --color-text: #1f2937;
  --color-text-muted: #6b7280;

  /* Fonts */
  --font-accent: 'Playfair Display', serif;
  --font-body: 'Inter', sans-serif;
}
```

## Теми (тільки форми)

Тема визначає **візуальний стиль форм**, але НЕ кольори:

| Тема | border-radius | Тіні | Hero стиль |
|------|---------------|------|------------|
| **Cosmetology** | 24px (м'які) | Розмиті, легкі | gradient |
| **Medical** | 4px (гострі) | Чіткі, мінімальні | minimal |
| **Massage** | 16px | Середні | wave |
| **Sport** | 0px | Різкі, offset | diagonal |
| **Beauty** | 9999px (pill) | Glow-ефекти | gradient |
| **Wellness** | 12px | М'які | minimal |

### Приклад теми

```typescript
// site/src/lib/themes.ts
cosmetology: {
  id: 'cosmetology',
  name: 'Косметологія',
  borderRadius: {
    base: '16px',
    card: '24px',
    button: '12px',
    input: '8px',
  },
  shadow: {
    card: '0 10px 40px rgba(0, 0, 0, 0.06)',
    button: '0 4px 15px var(--color-primary-500)40',
    elevated: '0 20px 60px rgba(0, 0, 0, 0.08)',
  },
  heroStyle: 'gradient',
  cardStyle: 'glass',
  buttonStyle: 'gradient',
  backgroundPattern: 'none',
  spacing: 'spacious',
}
```

## Як це працює

### 1. Сторінка завантажує дані компанії

```typescript
// site/src/app/site/[slug]/page.tsx
const company = await publicApi.getCompany(slug)
```

### 2. Генерація CSS змінних

```typescript
const colorVars = generateCssVariables({
  primary: company.primary_color || '#e91e63',
  secondary: company.secondary_color || '#9c27b0',
  background: company.background_color || '#ffffff',
  accentFont: company.accent_font || 'Inter',
  bodyFont: company.body_font || 'Inter',
})
```

### 3. Інжекція в сторінку

```tsx
<style dangerouslySetInnerHTML={{ __html: cssVariables }} />
<main style={{ backgroundColor: 'var(--color-background)' }}>
  ...
</main>
```

### 4. Компоненти використовують змінні

```tsx
// До (хардкод)
<button style={{ backgroundColor: '#e91e63' }}>

// Після (CSS змінні)
<button style={{ backgroundColor: 'var(--color-primary-500)' }}>
```

## Файлова структура

```
site/src/
├── lib/
│   ├── colors.ts      # Генерація CSS змінних з кольорів
│   ├── themes.ts      # Теми (тільки форми, без кольорів)
│   └── api.ts         # API + Company interface
├── app/site/[slug]/
│   └── page.tsx       # Інжекція CSS змінних
└── components/sections/
    ├── HeroSection.tsx
    ├── ServicesSection.tsx
    ├── ContactSection.tsx
    └── MapSection.tsx
```

## Адмін панель

Налаштування в `/admin/website`:

```
┌─────────────────────────────────────────────────┐
│ 🎨 Кольори                                      │
├─────────────────────────────────────────────────┤
│ Основний колір   [■ #e91e63] [палітра...]       │
│ Вторинний колір  [■ #9c27b0] [палітра...]       │
│ Фон сторінки     [■ #ffffff] [палітра...]       │
├─────────────────────────────────────────────────┤
│ 🔤 Шрифти                                       │
├─────────────────────────────────────────────────┤
│ Заголовки        [Playfair Display ▼]           │
│ Основний текст   [Inter ▼]                      │
├─────────────────────────────────────────────────┤
│ 📐 Стиль форм                                   │
├─────────────────────────────────────────────────┤
│ ○ Косметологія (м'які форми)                    │
│ ○ Медицина (чіткі лінії)                        │
│ ○ Масаж (природні форми)                        │
│ ○ Спорт (динамічні кути)                        │
│ ○ Б'юті (елегантні пілюлі)                      │
│ ○ Веллнес (мінімалізм)                          │
└─────────────────────────────────────────────────┘
```

## База даних

```sql
-- companies table
primary_color     VARCHAR(7)   -- #e91e63
secondary_color   VARCHAR(7)   -- #9c27b0
background_color  VARCHAR(7)   -- #ffffff
accent_font       VARCHAR(100) -- Playfair Display
body_font         VARCHAR(100) -- Inter
industry_theme    VARCHAR(50)  -- cosmetology, medical, massage, sport, beauty, wellness
```

## Приклади використання CSS змінних

### Кнопка

```tsx
<a
  style={{
    backgroundColor: 'var(--color-primary-500)',
    color: 'var(--color-primary-contrast)',
    borderRadius: theme.borderRadius.button,
  }}
>
  Записатися
</a>
```

### Заголовок

```tsx
<h2
  style={{
    fontFamily: 'var(--font-accent)',
    color: 'var(--color-text)',
  }}
>
  Наші послуги
</h2>
```

### Картка

```tsx
<div
  style={{
    backgroundColor: 'var(--color-surface)',
    borderRadius: theme.borderRadius.card,
    boxShadow: theme.shadow.card,
    borderLeft: '4px solid var(--color-primary-500)',
  }}
>
  ...
</div>
```

### Бейдж/тег

```tsx
<span
  style={{
    backgroundColor: 'var(--color-primary-100)',
    color: 'var(--color-primary-700)',
  }}
>
  Прайс-лист
</span>
```

### Градієнт (Hero)

```tsx
<section
  style={{
    background: 'linear-gradient(135deg, var(--color-primary-500), var(--color-secondary-500))',
  }}
>
  ...
</section>
```

## Переваги підходу

1. **Гнучкість**: Кожна компанія має унікальні кольори
2. **Консистентність**: Відтінки генеруються автоматично
3. **Простота**: Один колір → вся палітра
4. **Розділення**: Форми (тема) окремо від кольорів
5. **Підтримка**: Легко додавати нові теми або компоненти
